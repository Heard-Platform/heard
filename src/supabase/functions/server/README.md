## Running Tests

Since these are Deno edge functions, you'll need to run the tests using Deno's test runner.

### Run All Tests

From the project root:

```bash
cd supabase/functions/server
deno test
```